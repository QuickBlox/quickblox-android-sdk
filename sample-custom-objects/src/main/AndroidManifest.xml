<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.quickblox.sample.customobjects">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".activities.SplashActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.DisplayNoteListActivity"
            android:label="@string/notes"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.AddNewNoteActivity"
            android:label="@string/add_new_note"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.ShowNoteActivity"
            android:label="@string/note_title"
            android:screenOrientation="portrait" />
    </application>
</manifest>