<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.quickblox.sample.customobjects">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">

        <activity
            android:name=".activities.SplashActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.MovieListActivity"
            android:label="@string/movies"
            android:screenOrientation="portrait" />

        <activity
            android:name=".activities.AddNewMovieActivity"
            android:label="@string/movie_add_new"
            android:screenOrientation="portrait" />

        <activity
            android:name=".activities.ShowMovieActivity"
            android:label="@string/movie_details"
            android:screenOrientation="portrait" />

    </application>
</manifest>